% Standard business letter: no running header/footer.
% The sender's contact block is handled by \address{} in main.tex instead
% of a resume-style banner, so this file just enforces a plain page style.
\pagestyle{empty}
\makeatletter
\let\ps@empty\ps@empty
\let\ps@firstpage\ps@empty
\makeatother
